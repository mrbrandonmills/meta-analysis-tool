# Production-only requirements (minimal for faster Railway builds)
# This removes heavy ML/scientific dependencies that aren't critical for MVP

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4  # Using argon2 instead of bcrypt (more secure, no 72-byte limit)
argon2-cffi==23.1.0  # Backend for passlib argon2
python-multipart==0.0.6
email-validator==2.1.0

# AI & LLM (REQUIRED)
anthropic==0.18.1
openai==1.12.0

# Database (REQUIRED)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
aiosqlite==0.19.0
redis==5.0.1
alembic==1.13.1

# Background Jobs
celery==5.3.4
redis[hiredis]==5.0.1

# Web Scraping (needed for PubMed search)
httpx==0.26.0
beautifulsoup4==4.12.2
requests==2.31.0

# Utilities (REQUIRED)
python-dotenv==1.0.0
loguru==0.7.2
pyyaml==6.0.1
python-multipart==0.0.6
aiofiles==23.2.1

# Optional: LangChain (can be added back if needed)
# langchain==0.1.9
# langchain-anthropic==0.1.4
# langgraph==0.0.25

# Statistical & Scientific Computing (REQUIRED for meta-analysis)
# These are CRITICAL for Tool 1 - cannot perform meta-analysis without them
numpy==1.26.2
scipy==1.11.4
pandas==2.1.4
statsmodels==0.14.1
scikit-learn==1.4.0

# Visualization (REQUIRED for forest plots and figures)
matplotlib==3.8.2
seaborn==0.13.1

# NOTE: Other heavy dependencies removed for faster builds:
# - chromadb (vector database - not used in MVP)
# - sentence-transformers (ML models - not used in MVP)
# - spacy, nltk (NLP - not critical for MVP)
# - pymupdf, pdfplumber, pypdf (PDF processing - future feature)
# - pytest, black, flake8, mypy (development only - use requirements-test.txt)
